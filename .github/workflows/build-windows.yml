name: Build Windows Executable

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller
    
    - name: Build Windows executable
      run: |
        pyinstaller --onefile --windowed --name "StatisticalCalculator" --add-data "import streamlit as st.py;." launcher.py
      
    - name: Upload executable
      uses: actions/upload-artifact@v4
      with:
        name: StatisticalCalculator-Windows
        path: dist/StatisticalCalculator.exe
        retention-days: 30
